import 'package:flutter/material.dart';

class AppPath {
  static const nameSuffixIcon = Icon(Icons.person_add);
  static const lastNameSuffixIcon = Icon(Icons.person_add);
  static const passwordSuffixIcon = Icon(Icons.visibility);
  static const emailSuffixIcon = Icon(Icons.email_outlined);

  static const elvatedButtonIcon = Icon(Icons.app_registration);
}
